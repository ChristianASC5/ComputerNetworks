def bits(bytes: int) -> int:
    """
    Converts bytes to bits.

    Parameters:
    - bytes (int): The number of bytes to be converted.

    Returns:
    - The value after unit conversion.
    """
    return bytes * 8


def metric(num, string):
    """
    Converts bytes to bits.

    Parameters:
    - bytes (int): The number of bytes to be converted.

    Returns:
    - The value after unit conversion.
    """
    return
